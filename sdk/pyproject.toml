[project]
name = "labflow"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "requests>=2.32.0",
    "psutil>=6.0.0",
    "nvidia-ml-py>=12.535.77"
]

[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"
